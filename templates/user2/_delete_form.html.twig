<form method="post" action="{{ path('app_user2_delete', {'id': user2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user2.id) }}">
    <button class="btn">Delete</button>
</form>
